services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEMO_MODE=true
    restart: unless-stopped
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:
